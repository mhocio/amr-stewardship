## Instructions for collaborators
- Follow the README for setup (Docker Compose or local MySQL).
- Prefer `npm ci` for frontend installs and `mvn test` for backend checks.
- Keep documentation current when adding configuration or environment steps.
