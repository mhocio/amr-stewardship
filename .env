# Environment variables for the application
# Backend port (exposed on host)
BACKEND_PORT=8081

# Frontend port (exposed on host)
FRONTEND_PORT=9090

# API base URL for the frontend (external host URL)
API_BASE_URL=http://localhost:${BACKEND_PORT}/api